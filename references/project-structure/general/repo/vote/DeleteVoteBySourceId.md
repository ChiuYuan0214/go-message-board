# VoteImpl.DeleteVoteBySourceId

**File:** `backend/general/repo/vote.go`
**Struct:** `VoteImpl`

```go
func (r *VoteImpl) DeleteVoteBySourceId(source enum.VoteSource, sourceId uint64) (err error)
```

- Repo method on `VoteImpl` for the `vote` persistence path in general service.
