# ProfileImpl.GetByUserId

**File:** `backend/general/repo/profile.go`
**Struct:** `ProfileImpl`

```go
func (r *ProfileImpl) GetByUserId(userId uint64) (profile types.Profile, err error)
```

- Repo method on `ProfileImpl` for the `profile` persistence path in general service.
