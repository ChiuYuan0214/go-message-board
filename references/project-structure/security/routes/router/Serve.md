# RouterImpl.Serve

**File:** `backend/security/routes/base.go`
**Struct:** `RouterImpl`

```go
func (r *RouterImpl) Serve()
```

- Route handler method on `RouterImpl` for security service request handling.
