# UserImpl.ListUsers

**File:** `backend/security/repo/user.go`
**Struct:** `UserImpl`

```go
func (r *UserImpl) ListUsers() ([]store.User, error)
```

- Repo method on `UserImpl` for the `user` data path in security service.
