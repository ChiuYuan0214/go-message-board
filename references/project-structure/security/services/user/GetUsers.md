# UserImpl.GetUsers

**File:** `backend/security/services/common.go`
**Struct:** `UserImpl`

```go
func (s *UserImpl) GetUsers(name string, userId uint64) []store.User
```

- Service method on `UserImpl` coordinating `user` behavior in security service.
